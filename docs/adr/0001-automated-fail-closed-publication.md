# Automate publication and fail closed

BetterCallSol publishes Solutions without human review so the index can remain fully automated. A Solution is published only when rank, team attribution, and source evidence can be verified; ambiguous or conflicting records are quarantined and retried when later syncs provide new evidence, because silently omitting uncertain data is preferable to presenting an unearned ranking as fact.
